apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lil-auth.fullname" . }}-test-connection"
  labels:
{{ include "lil-auth.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "lil-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
